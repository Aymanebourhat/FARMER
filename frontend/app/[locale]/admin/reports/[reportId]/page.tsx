import { AdminReportDetailScreen } from "@/components/admin/listings-reports"; import { marketplacePageShell } from "@/components/marketplace/page-shell"; export default function Page({params}:{params:Promise<{locale:string;reportId:string}>}){return marketplacePageShell(params,async(locale,dictionary)=>{const{reportId}=await params;return <AdminReportDetailScreen locale={locale} dictionary={dictionary} reportId={reportId}/>;});}
