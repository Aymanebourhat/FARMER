import { AdminNav } from "@/components/admin/common"; import { AdminVetReviewScreen } from "@/components/vets/vet-screens"; import { marketplacePageShell } from "@/components/marketplace/page-shell"; export default function Page({params}:{params:Promise<{locale:string;vetId:string}>}){return marketplacePageShell(params,async(locale,dictionary)=>{const{vetId}=await params;return <div className="space-y-6"><AdminNav locale={locale} t={dictionary.admin}/><AdminVetReviewScreen locale={locale} dictionary={dictionary} vetId={vetId}/></div>;});}
