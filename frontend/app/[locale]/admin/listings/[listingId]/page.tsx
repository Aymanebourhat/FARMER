import { AdminListingDetailScreen } from "@/components/admin/listings-reports"; import { marketplacePageShell } from "@/components/marketplace/page-shell"; export default function Page({params}:{params:Promise<{locale:string;listingId:string}>}){return marketplacePageShell(params,async(locale,dictionary)=>{const{listingId}=await params;return <AdminListingDetailScreen locale={locale} dictionary={dictionary} listingId={listingId}/>;});}
