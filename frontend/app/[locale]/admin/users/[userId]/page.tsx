import { AdminUserDetailScreen } from "@/components/admin/dashboard-users"; import { marketplacePageShell } from "@/components/marketplace/page-shell"; export default function Page({params}:{params:Promise<{locale:string;userId:string}>}){return marketplacePageShell(params,async(locale,dictionary)=>{const{userId}=await params;return <AdminUserDetailScreen locale={locale} dictionary={dictionary} userId={userId}/>;});}
